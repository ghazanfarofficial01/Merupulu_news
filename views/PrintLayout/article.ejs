<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>News Article</title>
    <style>
      @font-face {
        font-family: 'NotoSansDevanagari';
        src: url('NotoSansDevanagari-Regular.ttf')  format('truetype');
}
      body {
        font-family: TeluguFont, sans-serif;
        font-size: smaller;
        margin: 0;
        padding: 0;
        background-color: #f2f2f2;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
      }
      .article {
        width: 90%; /* Adjust the width as desired */
        max-width: 1200px; /* Set a maximum width for larger screens */
        background-color: #fff;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        border-radius: 10px;
        overflow: hidden;
        margin-bottom: 20px;
        padding: 24px;
      }
      .article img {
        object-fit: cover;
        max-width: 50%;
        /* height: 50%; */
        display: block;

        max-width: 100%;

        display: block;
        width: 100%;
        height: 20em;
      }

      .article-content {
        padding: 20px;
      }
      .article h1 {
        font-size: 28px;
        margin-bottom: 10px;
      }
      .article p {
        font-size: 18px;
        line-height: 1.6;
        color: #555;
      }
      .author {
        margin-top: 15px;
        font-style: italic;
        color: #888;
      }
    </style>
  </head>
  <body>
    <% for (let article of articles) { %> <% url =
    "https://media.istockphoto.com/id/1212012012/vector/breaking-news-isolated-vector-icon-sign-of-main-news-on-dark-world-map-background.jpg?s=2048x2048&w=is&k=20&c=dLWAv64dLZtSHnOm2hbD96oWpuCqaGYDz18oJPxoEdk="
    %> <% if(article.url != "") {%> <% url = article.url %> <% } %>
    <div class="article">
      <img src="<%= url %>" alt="News Image" />
      <div class="article-content">
        <h1><%= article.title %></h1>
        <p><%= article.description %></p>
        <div class="author"><%= article.author %></div>
      </div>
    </div>
    <% } %>
  </body>
</html>
